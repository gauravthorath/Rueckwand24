export type CircleData = {
	id: string;
	x: number;
	y: number;
	radius: number;
};

export type Material = "glass" | "aluminium" | "steel" | "wood" | "stone";

export type Point = { x: number; y: number };
